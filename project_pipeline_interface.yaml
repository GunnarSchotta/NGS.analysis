pipeline_name: NGS.analysis
output_schema: pipestat_results_schema.yaml

project_interface:
  command_template: >
    python /store24/project24/becgsc_001/dev/NGS.analysis.v2/NGS.analysis/NGS.analysis.collator.py
    --config {looper.pep_config}
    -O {looper.output_dir}
    -P {compute.cores}
    -M {compute.mem}
    -n {project.name}
    -r {looper.output_dir}

# Submission resources for the project job (SLURM)
compute:
  mem: 20G
  cores: 32
  partition: slim16
  time: 08:00:00